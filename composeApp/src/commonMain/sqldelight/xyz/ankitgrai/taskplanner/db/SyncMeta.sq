CREATE TABLE SyncMetadata (
    id INTEGER NOT NULL PRIMARY KEY DEFAULT 1,
    last_sync_timestamp TEXT
);

getLastSync:
SELECT last_sync_timestamp FROM SyncMetadata WHERE id = 1;

updateLastSync:
INSERT OR REPLACE INTO SyncMetadata (id, last_sync_timestamp) VALUES (1, ?);
